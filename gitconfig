[user]
	name = Brice Letcher
	email = brice.letcher@ens-lyon.fr
[credential]
	helper = store
[core]
	editor = nvim

# Below line, when cloning a https repo, will convert the remote link from https 
# to git@github.com - i.e., to SSH.
# However, this affects everything that tries to use git;
# for example, vim (and other tools) will try to clone plugins using SSH, and
# notably, causes a rust cargo issue. (solved below, from https://github.com/rust-lang/cargo/issues/3381)
# So is disabled.
#[url "git@github.com/"]
#	insteadOf = https://github.com/

[init]
	defaultBranch = main
[pull]
	rebase = true
[safe]
	directory = /media/adminbrice/delattre_smb/sequencing_metadata
