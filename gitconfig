[user]
	name = Brice Letcher
	email = bletcher@ebi.ac.uk
[credential]
	helper = store
[core]
	editor = nvim

# force authentication to use SSH rather than HTTPS.
# be aware this affects everything that tries to use git.
# for example, vim (and other tools) will try to clone plugins using SSH.
# notably, causes a rust cargo issue, solved below, from https://github.com/rust-lang/cargo/issues/3381
#[url "git@github.com/"]
#	insteadOf = https://github.com/

[init]
	defaultBranch = main
